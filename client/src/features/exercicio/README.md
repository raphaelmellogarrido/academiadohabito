# Exercício

Hábito bloqueado (`status: "em_breve"` em `server/src/modules/habits/habits.data.ts`).

Sem conteúdo/rotas ainda — feature nasce aqui (mesma estrutura de `../meditacao`:
`components/`, `pages/`, `hooks/`, `api/`) quando sair de "em_breve".
